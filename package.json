{
  "name": "jsboids",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build --base=/jsBoids/ && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^3.2.3"
  },
  "dependencies": {
    "bresenham": "^0.0.4",
    "gh-pages": "^4.0.0"
  }
}
